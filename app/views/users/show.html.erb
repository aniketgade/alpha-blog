<h1 class="text-center mt-4"><%= t('.profile',user: @user.username)%></h1>

<div class="apple_pagination">
  <%= will_paginate @articles, :container => false %>
</div>

<%= gravatar_for @user, size: 200 %>

<div class="text-center mt-4">
  <%= link_to t('.edit_your_profile'), edit_user_path(@user), class: "btn btn-outline-info" %>
</div>

<h3 class="text-center mt-4"><%= t('layouts.navigation.articles')%></h3>

<%= render 'articles/article' %>

<div class="apple_pagination">
  <%= will_paginate @articles, :container => false %>
</div>